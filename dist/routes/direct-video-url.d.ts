import { FastifyInstance } from 'fastify';
/**
 * Direct Video URL Ingestion endpoint
 * POST /api/ai/video/url
 * Accepts any public video URL and dispatches processing job
 */
export default function directVideoURLRoutes(fastify: FastifyInstance): Promise<void>;
//# sourceMappingURL=direct-video-url.d.ts.map