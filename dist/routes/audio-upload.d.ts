import { FastifyInstance } from 'fastify';
/**
 * Audio Upload endpoint
 * POST /api/ai/audio/upload
 * Accepts audio file upload, stores in Supabase Storage, and dispatches processing job
 */
export default function audioUploadRoutes(fastify: FastifyInstance): Promise<void>;
//# sourceMappingURL=audio-upload.d.ts.map