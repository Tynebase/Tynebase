import { FastifyInstance } from 'fastify';
/**
 * Video Upload endpoint
 * POST /api/ai/video/upload
 * Accepts video file upload, stores in Supabase Storage, and dispatches processing job
 */
export default function videoUploadRoutes(fastify: FastifyInstance): Promise<void>;
//# sourceMappingURL=video-upload.d.ts.map