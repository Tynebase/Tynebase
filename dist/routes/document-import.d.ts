import { FastifyInstance } from 'fastify';
/**
 * Document Import endpoint
 * POST /api/documents/import
 * Accepts document file upload (PDF, DOCX, MD), stores in Supabase Storage, and dispatches conversion job
 */
export default function documentImportRoutes(fastify: FastifyInstance): Promise<void>;
//# sourceMappingURL=document-import.d.ts.map