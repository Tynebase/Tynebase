import { FastifyInstance } from 'fastify';
/**
 * Super Admin User Management Routes
 *
 * Provides platform-wide user management capabilities:
 * - List all users across all tenants
 * - Delete users
 * - Send password recovery emails
 * - Assign AI credits to tenant credit pools
 */
export default function superAdminUsersRoutes(fastify: FastifyInstance): Promise<void>;
//# sourceMappingURL=superadmin-users.d.ts.map